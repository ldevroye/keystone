QEMU: apt-get install qemu-system
Keystone: https://keystone-enclave.org
SDK: https://github.com/keystone-enclave/keystone-sdk

steps: 
a) clone git recursive (long)
b) download qemu
c) make -j$(nproc) (long)
d) make run
e) exit with 'poweroff'

DEBUG:
in keystone dir
$KEYSTONE_DEBUG=y make run
$make debug-connect

Notes:
1. examples/hello and not sdk/examples/hello
2. make run -> 	user: root
		pswd: sifive
3. make -j$(nproc)
4. fast-setup.sh

INSTALLING SDK
5. 
mkdir build
cd build
export KEYSTONE_SDK_DIR=<install_directory> # add to 'nano ~/.bashrc'
cmake ..
make

('sudo chown louis:louis ~/keystone-sdk/sdk/install_manifest.txt' if necessary	)
make install

6.
add <cstdint>

7.
replace 'sbadaddr' by 'stval' in several 'entry.S'

8. 
make examples
make -j$(nproc)

9. Follow doc : 
make hello-package

10. READ THE READMEs

REBUILD the examples:
$BUILDROOT_TARGET=keystone-examples-dirclean make -j$(nproc)

